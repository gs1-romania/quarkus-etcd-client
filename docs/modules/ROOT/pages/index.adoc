= Quarkus - Etcd
:extension-status: preview

Describe what the extension does here.

== Installation

If you want to use this extension, you need to add the `io.quarkiverse.quarkus-etcd:quarkus-etcd` extension first.
In your `pom.xml` file, add:

[source,xml]
----
<dependency>
    <groupId>io.quarkiverse.quarkus-etcd</groupId>
    <artifactId>quarkus-etcd</artifactId>
</dependency>
----

[[extension-configuration-reference]]
== Extension Configuration Reference

include::config.adoc[leveloffset=+1, opts=optional]
